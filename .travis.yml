language: python
dist: trusty
python:
    - "2.7"
addons:
  apt:
    packages:
      - gfortran
      - libblas-dev
      - liblapack-dev
      - libopenmpi-dev
      - openmpi-bin
env:
     - PARA=no
install:
     - ./configure --enable-parallel=$PARA --with-netlib
script:
     - make all
git:
  depth: 3

